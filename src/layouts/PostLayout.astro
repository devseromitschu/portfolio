---
import "@fontsource/fira-mono";
import Layout from "./Layout.astro";
const { title, description, date, author } = Astro.props.frontmatter;
---

<Layout title={`${title} • 23.dev`} description={description}>
  <div class="mt-3 mb-7">
    <h1 class="rounded-3xl border border-white/10 bg-white/5 p-5 text-4xl font-bold tracking-tight sm:text-5xl">
      {title}
    </h1>
  </div>
  <article class="prose prose-invert font-fira pb-10">
    <slot />
  </article>
  <p class="pb-5 mt-2 text-sm font-light text-gray-100">
     ✏️ <span class="italic">{author && `${author}`}</span>
     <br>
     🗓️ <span class="italic">{date}</span>
  </p>
</Layout>
